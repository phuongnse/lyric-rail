{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "player-main",
  "description": "Minimal dialog capability for the LyricRail main window; processing, keys, OAuth and media remain native",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
