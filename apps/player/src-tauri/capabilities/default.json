{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "player-main",
  "description": "Minimal read-only capability for the LyricRail Player main window",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:allow-open"
  ]
}
