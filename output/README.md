# Output workspace

LyricRail writes generated karaoke jobs, review media, delivery files, and
encrypted `.lrail` packages to this directory when no separate
`LYRICRAIL_DATA_HOME` is configured.

Generated contents are local artifacts and must not be committed. Keep this
README so a clean source checkout still contains the directory required by the
project validation contract.
